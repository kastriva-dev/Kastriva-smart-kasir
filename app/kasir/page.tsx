 "use client";
import {useState} from "react";
export default function Kasir(){const [wa,setWa]=useState(process.env.NEXT_PUBLIC_CASHIER_WHATSAPP||"6281234567890");const base=typeof window!=="undefined"?window.location.origin:"https://domain-anda.vercel.app";const qr=base+"/customer/kedai-saya/meja-01";return <main className="container"><div className="topbar"><div><h1>Dashboard Kasir</h1><p className="muted">Kelola POS dan QR customer.</p></div><a className="btn secondary" href="/">Beranda</a></div><div className="grid" style={{gridTemplateColumns:"repeat(auto-fit,minmax(300px,1fr))"}}><div className="card"><h2>WhatsApp Kasir</h2><p className="muted">Nomor tujuan link WhatsApp, format internasional tanpa +.</p><input className="input" value={wa} onChange={e=>setWa(e.target.value)}/><p className="muted" style={{marginTop:12}}>Atur permanen lewat <code>NEXT_PUBLIC_CASHIER_WHATSAPP</code> di environment.</p></div><div className="card"><h2>QR Meja 01</h2><p className="qr">{qr}</p><a className="btn" href={qr} target="_blank">Tes QR / Menu</a><p className="muted">Untuk produksi, URL ini dapat dijadikan QR menggunakan generator QR apa pun.</p></div></div></main>}